function TapRecognizer(node) {
  this.prototype = new GestureRecognizer(this, node);

  this.touchStart = function(event) {
  }

  this.touchMove = function(event) {
  }

  this.touchEnd = function(event) {
  }
}
